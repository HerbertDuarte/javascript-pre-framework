function maiusculo(name){
  return name.toUpperCase()
}
// testando no console do navegador vemos que a função já está funcionando
// podemos também declarar uma const com a função dentro

const upperName = function(name){
  return name.toUpperCase()
}

// testando no console do navegador vemos que a função já está funcionando

